<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.csedu.mediico">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".my_project.activities.pharmacy_entry"></activity>
        <activity android:name=".my_project.activities.admin_general" />
        <activity android:name=".my_project.activities.admin_medicine_reg" />
        <activity android:name=".my_project.activities.MainActivity" />
        <activity android:name=".my_project.activities.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".my_project.activities.userActivity" />
        <activity android:name=".my_project.activities.DeliverymanActivity" />
    </application>

</manifest>